# from flask_wtf import FlaskForm
# from wtforms import StringField
# from wtforms.validators import DataRequired, Length
# from app.models import ServiceImage

# class ServiceImageForm(FlaskForm):
#     url = StringField('service_image_url', validators=[DataRequired(), Length(min=1, max=255, message='Service Image URL must be between 1 and 255 characters')])
